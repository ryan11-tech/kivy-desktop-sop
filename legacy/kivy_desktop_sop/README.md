# Legacy Kivy Desktop SOP

Archived Kivy implementation from the previous `kivy-desktop-sop` project.

Run from this folder only if you need to inspect the old app:

```sh
pip install kivy
python main.py
```

Default PINs:

- Staff: `2222`
- Admin: `1234`

New product work should happen in the Flutter app at the repository root.
